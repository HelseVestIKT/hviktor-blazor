@namespace List
@inherits Hviktor.Models.List.ListBase

<ul @attributes="@(ComputeAttributes())">
    @if (ChildContent is not null)
    {
        <CascadingValue Value="@this">
            @ChildContent
        </CascadingValue>
    }
</ul>