<select @attributes="@(ComputeAttributes())">
    @if (ChildContent is not null)
    {
        <CascadingValue Value="@(ComputeAttributes())">
            @ChildContent
        </CascadingValue>
    }
</select>
