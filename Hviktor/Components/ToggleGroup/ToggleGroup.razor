@inherits Hviktor.Models.AsyncCascadingComponentBase

<fieldset @ref="ElementRef" @attributes="@(ComputeAttributes())">
    @if (ChildContent is not null)
    {
        <CascadingValue Value="@this">
            @ChildContent
        </CascadingValue>
    }
</fieldset>